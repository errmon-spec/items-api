{
  "extends": "stylelint-config-standard",
  "ignoreFiles": ["app/assets/builds/**"],
  "rules": {
    "at-rule-no-unknown": [
      true,
      {
        "ignoreAtRules": ["tailwind"]
      }
    ]
  }
}
